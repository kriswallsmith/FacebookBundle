<?xml version="1.0" ?>

<container xmlns="http://www.symfony-project.org/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.symfony-project.org/schema/dic/services http://www.symfony-project.org/schema/dic/services/services-1.0.xsd">

  <parameters>
    <!-- parameters used in both the js and php api classes -->
    <parameter key="facebook.app_id">null</parameter>
    <parameter key="facebook.cookie">false</parameter>

    <!-- parameters used by the php api -->
    <parameter key="facebook.class">Facebook</parameter>
    <parameter key="facebook.secret">null</parameter>
    <parameter key="facebook.domain">null</parameter>

    <!-- parameters used by the js api -->
    <parameter key="facebook.logging">true</parameter>
    <parameter key="facebook.xfbml">false</parameter>
    <parameter key="facebook.culture">en_US</parameter>
  </parameters>

  <services>
    <service id="facebook" class="%facebook.class%">
      <argument type="collection">
        <argument key="appId">%facebook.app_id%</argument>
        <argument key="secret">%facebook.secret%</argument>
        <argument key="cookie">%facebook.cookie%</argument>
        <argument key="domain">%facebook.domain%</argument>
      </argument>
    </service>
  </services>
</container>
