<fb:login-button show-faces="{{ showFaces }}" width="{{ width }}" max-rows="{{ maxRows }}" scope="{{ scope }}" registration-url="{{ registrationUrl }}" size="{{ size }}" autologoutlink="{{ autologoutlink }}" onlogin={{ onlogin }}>{{ label }}</fb:login-button>
