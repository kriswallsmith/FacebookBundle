<div class="fb-login-button" data-show-faces="{{ showfaces }}" size="{{ size }}" scope="{{ scope }}" autologoutlink="{{ autologoutlink }}" data-width="{{ width }}" data-max-rows="{{ maxRows }}">
  {{ label }}
</div>
